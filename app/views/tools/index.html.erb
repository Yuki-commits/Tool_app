<div class="mx-3 mt-4 mb-3 p-4 rounded-lg bg-white-80 border-green1 content-box">
  <div class="mb-4 d-flex align-items-center">
    <h5 class="m-0 mr-auto text-green2">工具一覧</h5>
    <% if !viewer?(current_user) %>
      <%= link_to new_tool_path, class:"mr-1 btn btn-sm btn-success btn-width" do %>
        <%= icon "fas", "plus" %>
        新規登録
      <% end %>
    <% end %>
    <%= link_to tools_path(format: :csv), class:"btn btn-sm btn-success btn-width" do %>
      <%= icon "fas", "download" %>
      CSV出力
    <% end %>
  </div>

  <%= render partial: "search_form", locals: { search_column: @search_column, search_value: @search_value } %>

  <table class="table table-striped table-sm mb-0 text-green2">
    <%= render 'table_head' %>
    <tbody>
      <%= render @tools if @tools.count > 0 %>
    </tbody>
  </table>
  <p><%= "工具が登録されていません" if @tools.count == 0 %></p>
  <%= paginate @tools %>

</div>
